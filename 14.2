#include <stdio.h>
#include <math.h>
#include <locale.h>


int main()
{
    setlocale(LC_ALL, "Rus");
    int a, b, i, j;

    printf("Введите a(A>B)\n");
    scanf_s("%d", &a);
    printf("Введите b\n");
    scanf_s("%d", &b);

    while (a >= b)
    {
        a -= b;
    }
    printf("%d", a);
    return 0;
}
