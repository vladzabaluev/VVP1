#include <stdio.h>
#include <math.h>
#include <locale.h>


int main()
{
	setlocale(LC_ALL, "Rus");
	double x;

	printf("vvedite x \n");
	scanf_s("%lf", &x);

	x = 3 * (pow(x, 6)) - 6 * x * x - 7;

	printf("x= %lf\n", x);

	return 0;
}
