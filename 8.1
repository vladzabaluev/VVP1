#include <math.h>
#include <stdio.h>
#include <locale.h>

int main()
{
	setlocale(LC_ALL, "RUS");
	int a;
    
	printf("Введите размер файла в байтах \n");
	scanf_s("%d", &a);
	
	a /= 1024;
	printf("Количество полных килобайт равно %d", a);
	return 0;
}
