#include <stdio.h>
#include <math.h>
#include <locale.h>


int main()
{
    setlocale(LC_ALL, "Rus");
    int a, b, i, j;

    printf("Введите a\n");
    scanf_s("%d", &a);

    b = 0;
    for (i = 0; b < a; i++) 
    {
        b += i;
    }
    printf("%d\n", i-1);
    printf("%d", b);

    return 0;
}
