#include <stdio.h>
#include <math.h>
#include <locale.h>

int Quarter(float x, float y)
{
    int chetvert;
    if (x > 0) 
    {
        if (y > 0) chetvert = 1;
        else if (y < 0) chetvert = 4;
    }
    else if (x < 0) 
    {
        if (y > 0) chetvert = 2;
        else if (y < 0) chetvert = 3;
    }
    
    return (chetvert);
}
int main()
{
    setlocale(LC_ALL, "Rus");
    float a, b; 
    for (int i = 0; i < 3; i++)
    {
        printf("Введите x \n");
        scanf_s("%f", &a);
        printf("Введите y \n");
        scanf_s("%f", &b);

        printf("Точка лежит в координатной четверти под номером : %d\n", Quarter(a, b));
    }


    return 0;
}
