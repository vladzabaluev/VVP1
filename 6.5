#include <stdio.h>
#include <math.h>
#include <locale.h>


int main()
{
	setlocale(LC_ALL, "Rus");
	double x;

	printf("vvedite x \n");
	scanf_s("%lf", &x);

	
	x = 4 * (pow(x - 3, 6)) - 7 * (pow(x - 3, 3)) + 2;

	printf("x= %lf\n", x);

	return 0;

}
