#include <stdio.h>
#include <math.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "Rus");
    double x1, y1, x2, y2, x3, y3;

    printf("Введите координаты a,b,c \n");
    scanf_s("%lf", &x1);
    scanf_s("%lf", &y1);
    scanf_s("%lf", &x2);
    scanf_s("%lf", &y2);
    scanf_s("%lf", &x3);
    scanf_s("%lf", &y3);

    x2 = sqrt((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1));
    x3 = sqrt((x3 - x1) * (x3 - x1) + (y3 - y1) * (y3 - y1));

    if (x2 > x3) printf("С ближе к A\nРасстояние между А и Б равно %lf", x3);
    if (x2 < x3) printf("В ближе к A\nРасстояние между А и С равно %lf", x2);
    return 0;

}
