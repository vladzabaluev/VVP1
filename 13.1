#include <stdio.h>
#include <math.h>
#include <locale.h>


int main()
{
    setlocale(LC_ALL, "Rus");
    float a; double i,b;

    printf("Введите стоимость 1кг конфет \n");
    scanf_s("%f", &a);

    for (i = 0.1, 1; i < 1; i = i + 0.1) 
    {
        printf("Стоимость %f ", i);
        b = double(i * a);
        printf(" кг конфет равна %lf\n",b);
    }
    return 0;
}
