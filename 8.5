#include <stdio.h>
#include <math.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "Rus");
    int a, b;

    printf("Введите число \n");
    scanf_s("%d", &a);

    b = a % 100;
    a /= 100;
    a = b * 10 + a;

    printf("Число полученное %d", a);
    return 0;
}
