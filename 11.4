#include <stdio.h>
#include <math.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "Rus");
    int a, b;

    printf("Введите координаты точки \n");
    scanf_s("%d", &a);
    scanf_s("%d", &b);

    if (a > 0)
    {
        if (b > 0)
        {
            printf("Точка находится в первой координатной плоскости");
        }
        else
        {
            printf("Точка находится во второй координатной плоскости");
        }
    }
    else if (b < 0)
    {
        printf("Точка находится в третьей координатной плоскости");
    }
    else printf("Точка находится в четвертой координатной плоскости");
    
    return 0;
}
