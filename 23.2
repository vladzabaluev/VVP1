#include <iostream>
#include <string>     
#include <windows.h> 

using namespace std;

int main()
{
    SetConsoleCP(1251); 
    SetConsoleOutputCP(1251); 

    cout << "Введите строку" << endl;
    string str;
    getline(cin, str);
    for (int i = 0; i < str.length(); i++)
        cout << str[i] << " ";
    cout << endl;

    return 0;
}
