#include <stdio.h>
#include <math.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "Rus");
    int a, b, t;

    printf("Введите а и б\n");
    scanf_s("%d", &a);
    scanf_s("%d", &b);

    if (a == b)
    {
        a = 0;
        b = 0;
    }
    if (a > b)
    {
        b = a;
    }
    if (a < b)
    {
        a = b;
    }
   
    printf("a= %d \n", a);
    printf("b= %d \n", b);
    return 0;
}
