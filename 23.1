#include <iostream>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");
    char C;
    cout << "Введите символ" << endl;
    cin >> C;
    cout << char(C - 1) << " " << char(C + 1);
    return 0;
}
